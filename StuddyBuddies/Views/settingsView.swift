

import SwiftUI

struct settingsView: View {
    var body: some View {
        Text("Settings View")
            .font(.system(size: 40))
    }
}

struct settingsView_Previews: PreviewProvider {
    static var previews: some View {
        settingsView()
    }
}
